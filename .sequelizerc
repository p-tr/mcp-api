//  This file contains the sequelize configuration, to be used with sequelize-cli.

require('dotenv').config();
require('module-alias/register');

const path = require('path');

module.exports = {
  config: path.resolve('config', 'sequelize.js')
}
